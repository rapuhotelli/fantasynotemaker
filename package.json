{
  "name": "fantasynotemaker",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "images": "cp assets/*.jpg docs",
    "dev": "parcel -p 1234 watch index.html",
    "build": "rm -rf docs && mkdir -p docs && npm run images && parcel build index.html --out-dir docs --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pixi.js": "^5.3.0",
    "webfontloader": "^1.6.28"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "typescript": "^3.9.6"
  }
}
