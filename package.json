{
  "name": "fantasynotemaker",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "images": "mkdir -p dist && cp assets/*.jpg dist",
    "dev": "parcel -p 1234 watch index.html",
    "build": "rm -rf dist && mkdir dist && npm run images && parcel build index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pixi.js": "^5.3.0",
    "webfontloader": "^1.6.28"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "typescript": "^3.9.6"
  }
}
